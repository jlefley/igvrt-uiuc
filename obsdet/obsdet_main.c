#include "obsdet.h"
#include <stdio.h>

int main(int argc, char** argv)
{
	detect_obstacles();
//	llnode * head = single_frame_detect_obstacles();
//	llnode * current = head;
//	while(current!=NULL)
//	{
//		printf("(%d,%d),(%d,%d) \n", current->x1, current->y1, current->x2, current->y2);
//		current = current->next;
//	}
//	current = head;
//	while(current!=NULL)
//	{
//		current = current->next;
//		free(head);
//		head = current;
//	}
}
